# создана функция "test_sum" с пустым аргументом, в котором сравнивается a и b переменные (что они равны)
def test_sum_neg():
    a = 5
    b = 10
    assert a == b

# создана функция "test_sum" с пустым аргументом, в котором сравнивается a и b переменные (что они не равны)
def test_sum():
    a = 5
    b = 10
    assert a != b

